port: 3000
mongodb:
  connection: "mongodb://localhost:27017/express-auth-demo"
session:
  secret: "express-auth-demo-session-secret"
  cookie:
    maxAge: 60000 # 60 * 1000
  mongodb:
    connection: "mongodb://localhost:27017/express-auth-demo-session"
    collection: "session"
